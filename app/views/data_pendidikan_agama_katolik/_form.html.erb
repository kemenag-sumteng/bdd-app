<%= bootstrap_form_for(@data_pendidikan_agama_katolik, local: true) do |form| %>
  <div class="card-body">
    <div class="field">
      <%= form.text_field :judul %>
    </div>
    <div class="field">
      <%= form.text_area :tautan %>
    </div>
    <div class="actions">
      <%= form.submit "Membuat Data Pendidikan Agama Katolik", class: "btn btn-success" %>
    </div>
  </div>
<% end %>
